<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_day_layout"
    android:layout_width="40dp"
    android:gravity="center"
    android:layout_height="40dp"
    android:layout_marginTop="15dp"
    android:layout_marginRight="5dp">

    <ImageView
        android:id="@+id/diary_exist_iv"
        android:layout_width="5dp"
        android:layout_height="5dp"
        android:src="@color/main_color_green"
        android:visibility="invisible"
        app:layout_constraintBottom_toTopOf="@id/item_day_tv"
        app:layout_constraintStart_toStartOf="@id/item_day_tv"
        app:layout_constraintEnd_toEndOf="@id/item_day_tv"/>

    <TextView
        android:id="@+id/item_day_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="9"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>